const socketIo = require("socket.io");

const io = socketIo();

// io.on("connect", (socket) => {
//   // const messages = await getUnreadMessages();
//   // socket.emit("getUnread", messages);
//   socket.on("sendMessage", (message) => {

//   })

// })
